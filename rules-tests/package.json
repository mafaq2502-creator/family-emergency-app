{
  "name": "alivecircle-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --config ../firebase.json --project demo-alivecircle --only firestore \"node --test --test-concurrency=1 firestore.rules.test.cjs trusted-boundary.rules.test.cjs device.rules.test.cjs\"",
    "test:lifecycle": "firebase emulators:exec --config ../firebase.json --project demo-alivecircle --only auth,firestore,functions \"node --test circle-lifecycle.integration.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.29.0"
  }
}
