{
  "name": "alivecircle-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --config ../firebase.json --project demo-alivecircle --only firestore \"node --test firestore.rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.29.0"
  }
}
